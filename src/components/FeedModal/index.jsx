import React from 'react'

import {Container} from './styles'

export function FeedModal() {
    const { } = useQuery(['photo', photo.id], () => [])
    
    return (
        <Container>

        </Container>
    )
}